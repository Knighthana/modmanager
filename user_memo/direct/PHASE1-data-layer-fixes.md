# Phase 1 — 数据层基线（已落地）

> Status: aligned
> Authority: user-guidance
> Purpose: 保留数据层改造意图，并与当前实现契约保持一致

---

## 一、目标

本阶段核心是清理数据层历史包袱，保证后续 API/前端调用口径一致。

当前目标已吸收为三条长期约束：
1. 配置与数据库路径解析走统一流程。
2. 规则聚合不再依赖旧式路径注入参数。
3. 业务流程中不再依赖 workspace 后端模块。

---

## 二、当前基线

1. bootstrap 与配置发现流程已稳定。
2. database 名称语义与前端调用保持一致。
3. 规则聚合入口与 pipeline 入口按现行契约运行。

---

## 三、implement guardrails

1. 不恢复 `user_config_path` 透传模式。
2. 不恢复旧缓存参数作为外部 API 输入。
3. 不重新引入 workspace 后端模块依赖链。
4. 数据层新增字段前，先更新 repo_memo 字段冻结文档。

---

## 四、增量改动流程

1. 先更新设计契约。
2. 再改数据层函数签名与调用链。
3. 最后补测试并跑门禁。

---

## 五、门禁

1. Python 测试通过。
2. 前端测试与构建通过。
3. 数据层改动与 repo_memo 无冲突。
