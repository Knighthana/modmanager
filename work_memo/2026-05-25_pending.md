# 2026-05-25 — 悬空问题与当前决定

## 一、可立即推进

### 编码器层错误

| # | 位置 | 问题 | 修复方向 |
|---|------|------|---------|
| BUG-1 | `backup_ops.py:639` | `_list_orphans()` 被调用但从未定义 | 实现函数——遍历 backup_dir 找不在 target_set 中的孤立文件 |
| BUG-2 | `routes/pipeline.py:107` | restore SSE 不转发 `on_progress` | `do_work` 内传 `on_progress` 给 `restore_from_backup` |

### Decisions 过时

| # | decisions.md | 问题 | 修复方向 |
|---|-------------|------|---------|
| P11-GAP | P11 规则漏斗 | `rule_validator.py` / `path_normalizer.py` 存在但聚合器未调用 | `rule_aggregator.aggregate()` 入口调 `validate_kmm_rule_files()` + `normalize_rule_actions()` |
| P12-GAP | P12 bootstrap 不写文件 | `generate_database()` 仍在 bootstrap.py 中写磁盘 | 移入 `database_ops.py` |
| P21-GAP | P21 decisions.md | 引用了不存在的函数名 `_detect_platform_defaults()` | 改为 `osplatform.defaultvalue` |
| P22-GAP | P22 decisions.md | 说 required 含 `bakignore`（7 键），实际 6 键 | 更新为 6 键 |

---

## 二、kmmignore 复制/还原位置修正

### 当前（错误）

- 函数定义在 `orchestrator/__init__.py`
- `_dispatch_fileops` 直接调用
- `DESIGN_BACKUP_OPS.md:185` 写"调用点在 `_dispatch_fileops`"

### 目标

- **PLANNER** 在 `plan_fileops()` 时收集源目录各级 `.kmmignore` 路径，写入 `FileOpsPlan.kmmignore_files`
- **原语**执行复制——prep 将文件从源复制到 backup_dir，restore_ops 反向还原
- **编排器**不参与

### 需改动

| 文件 | 改动 |
|------|------|
| `planner_fileops.py` | `FileOpsPlan` 加 `kmmignore_files`；`plan_fileops` 中收集 |
| `prep.py` | `prep_backup_dir` 加 `.kmmignore` 复制（调用方传 kmmignore_files） |
| `restore_ops.py` | `restore_entries` 加 `.kmmignore` 还原 |
| `__init__.py` | 删 `_copy_kmmignore_to_backup/from_backup`；删 `_dispatch_fileops` 中的调用 |
| `DESIGN_BACKUP_OPS.md:185` | "调用点在 `_dispatch_fileops`" → "调用点在 Planner" |
| `repo_test/kmmignore_copy.md` | 更新测试断言 |
| `tests/test_kmmignore_copy.py` | 更新为 Planner 层测试 |

---

## 三、低优先清扫

| # | 位置 | 内容 |
|---|------|------|
| CODE-3 | `acf_parser.py` | `find_appmanifest_acf_files` / `find_appworkshop_acf_files` 零调用方——删除或标记 deprecated |
| CODE-4 | `routes/config.py:40` | `_normalize_rule_sources` 中 `isinstance(rs, list)` 分支永远不进——删除 |
| CODE-5 | `preflight.py` | `context` 参数从未使用——删参数 |
| CODE-6 | `bootstrap.py:146-147` | 注释残留 `source_path` / `first_use` → 改为 `config_index` |
| CODE-7 | `__init__.py` / `app.py` | 残留旧包名 `modmanager` → `modmgr` |

---

## 四、悬空 TASK2605（不在此轮推进）

| # | 内容 |
|---|------|
| 0x1 | 前端空输入校验 |
| 0x2 | 统一界面视觉效果 |
| 0x3 | 主题切换 |
| 0x4 | 日志文件 |
| 0x5 | preview 图片 |
| 0x6 | README 查看 |
| 0x7 | author 字段展示 |
| 0xA | 路径大小写 P1-P3 |
| 0xB | DESIGN_STORAGE 硬编码清理 |
