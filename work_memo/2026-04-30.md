# 2026-04-30 工作记录

## 完成事项

### 聚合器设计 + 实现
- 全面重写 RULE_AGGREGATION_DESIGN.md
- 废弃 source_trace_map，provenance_ref 固定为绝对路径
- 聚合器接管全部鉴权（sub + game[].modid）和 def_destin/def_action 继承
- M1 引擎移除 validate_forest_roots、sub 检查、继承逻辑
- rule_set 顶层 key：mod → operation
- sidecar_ref 保留外部注入接口
- 多规则合并策略：文件内具体化 → 跨文件合并 → 鉴权过滤
- 审计 5 个文件、修复 7 处冲突
- 实现 rule_aggregator.py（584 行），243 测试全通过

### 同 mod 冲突修复
- 同 mod 内多条 action 命中同一目标时自动取最后一条
- weaponpics 114 + shiplander 5 + bluearchive 1 = 120→0
- 诊断脚本 tools/diagnose_conflicts.py

### Bootstrap + Orchestrator 设计
- 拆分设计：bootstrap.py（初始化）+ orchestrator.py（调度入口）
- CLI 方案 A：orchestrator 为唯一调度入口，CLI 变薄壳
- 回调签名 (step, finished, total, message)
- 全部 8 个问题已决策

## 当前状态
- M1 引擎 + 补丁：完成
- 备份/替换/恢复：完成
- 聚合器：完成
- Forest 可视化：完成
- **Bootstrap + Orchestrator：完成 ✅（Phase 1）**
- **Web API 层：完成 ✅（Phase 2）**

## 下一阶段

### Phase 2 实现完成 ✅
- `modmanager_web` 独立子包（schemas + adapters + sse + routes + app + entry）
- FastAPI + SSE 进度推送 + ApiResponse 适配层
- `POST /api/pipeline/{compute,backup,apply,run}` + config/database 端点
- Q7 决策：方案 A（CLI 和 Web 独立对等，共享内核，最低耦合）
- 15 new tests，全量 276 通过，`modmanager_cli/*` 零改动

### Phase 3: 前端 GUI 完成 ✅
- Vue 3 + Vite + TypeScript + Element Plus + Pinia SPA
- 4 个页面：Forest 可视化 + 冲突裁决 + 规则浏览 + 备份恢复
- M3 SVG 升级：data-forest-node / data-conflict / title / desc 交互属性
- FastAPI StaticFiles + SPA fallback
- 前端 14 tests + Python 276 tests 全量通过

### 后续待推进
- Phase 4: 远程管理 / 认证 / 增强交互（M4: hover 高亮、拖拽选枝等）
