# 2026-04-30 工作记录

## 完成事项

### Phase 1: Bootstrap & Orchestrator ✅
- bootstrap.py + orchestrator.py + CLI 适配 + 18 tests
- 261 tests 通过

### Phase 2: Web API 层 ✅
- `modmanager_web` 独立子包，FastAPI + SSE，15 tests
- 全量 276 tests

### Phase 3: 前端 GUI ✅
- Vue 3 + Vite + TypeScript + Element Plus + Pinia
- 4 页面：Forest 可视化 / 冲突裁决 / 规则浏览 / 备份恢复
- M3 SVG 升级、FastAPI StaticFiles + SPA fallback
- 276 Python + 14 Vitest = 290 tests

### 项目重命名 ✅
- `modmanger-cli` → `modmanager`
- `src/modmanager_cli/` → `src/modmanager/`（git mv）
- 全量 import 路径更新、venv 重建
- 构建产物移至 `frontend/dist/`

### ForestPage 自动探测 ✅
- "🔍 自动探测 Steam 库"按钮 → SSE 扫描 → 填充 database
- `POST /api/config/save` 端点、user_config 自动回退

### Bug 修复
- **dry_run 写盘**：`orchestrator.run()` 中 dry_run 跳过 backup+apply
- **表单持久化**：ForestPage 表单从组件局部状态移到 Pinia store
- **错误展示**：新增可折叠的错误/警告面板（el-collapse）
- **Forest 图不显示**：`pipeline.py` 漏了 `adapt_dict_result` import → 修复
- **SVG 透明背景**：`forest_visual.py` 加 `bgcolor=transparent`
- **路径 `/` 丢失**：`normalize_posix` 去 `rstrip("/")`；聚合器自动补 `/` + `W_PATH_TRAILING_SLASH_FIXED`
- **W_NO_SOURCE_MATCH 诊断**：警告信息新增 `resolved:` 完整路径

### 操作废弃
- `rename_then_replace`、`clear_then_copy` 从 `VALID_ACTIONS` 移除
- `backup_ops` 中 `_collect_clear_then_copy_dirs` 删除
- 相关测试和数据模型更新

### 设计讨论固化
- **森林模型**：独立根 + 引用（`FOREST_DELETE_FORK_DESIGN.md`）
  - 废弃旧 fork 模型（filenotexist/holdasoriginal/delay-delete）
  - 从底向上解析算法、成环/断头检测、T8 布尔信号传递
  - 3 个引擎对接 GAP 标记待填
- **Backup 设计**：7 条原则确认（`BACKUP_DIR_BUILDER_DESIGN.md`）
  - bakprefix=`kmmbackup_`、workshop time 源、位置规则、`.kmmbakignore`
  - `user_config.json.example` 新增 bakprefix/bakignore 字段

---

## 下次开工计划

### P0: 森林模型引擎重构
- `FOREST_DELETE_FORK_DESIGN.md` §13 的 3 个 GAP
- 实现独立根+引用模型，替换旧级联 delete

### P1: Backup 实现
- builder 模块 → backup_dir 命名规则
- workshop time 源（appworkshop.acf → timeupdated）
- bakprefix/bakignore 读取与过滤
- .kmmbakignore 支持
- backup_ops 硬编码忽略 kmmbackup_

### P2: 引擎细节修复
- T1: same actionlist 中 delete→create 不产生 overwrite 警告
- 结点 vs 节点术语统一（代码变量名）

### P3: GUI（待用户反馈后决定）
- Forest 全部/仅分岔切换按钮
- M4: hover 高亮、拖拽选枝

---

## 今日提交链

```
c785a6b fix: W_NO_SOURCE_MATCH shows resolved path
9a9f80e docs: forest model independent-root + reference design
ccf299f docs: forest model evolution — root-transplant to reference
3d15314 docs: forest delete fork design draft + 7 challenges
26e72c9 refactor: deprecate rename_then_replace and clear_then_copy
95b1e52 fix: trailing slash + aggregator auto-completion
60fc7ff fix: Forest SVG transparent background
85e1806 docs: dry_run semantics, warnings reference, terminology
40eefb8 fix: Forest SVG rendering + terminology + warning explanations
2f06a5d fix: auto-generate backup_dir + fallback user_config
bbb6800 fix: persist form + show error/warning details
8dfecff feat: ForestPage auto-discovery
36bcdad fix: dry_run skips backup + compute/apply separation
2bc1aa4 refactor: rename project modmanger-cli → modmanager
dc96919 feat: Phase 3 GUI complete
c2c6160 docs: Phase 3 task list
8369d55 docs: Phase 3 design — 13 decisions
5b73914 feat: Phase 1 + Phase 2 complete
```
