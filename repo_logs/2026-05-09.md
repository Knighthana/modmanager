# 2026-05-09 工作记录

## 完成事项

### 阶段 A — Bug 修复（TODO-8/9/6/11/3）

- **TODO-8: ConflictsPage branch decisions 刷新丢失** → ForestStore 持久化。`persistence.ts` watch deep 同步 localStorage，页面刷新后自动恢复 branch decisions 状态，避免用户重复裁决。
- **TODO-9: onDbPathBlur 静默失败** → 失焦校验 + 错误气泡。ForestPage database 路径行 blur 时触发后端 `/api/database/load` 校验，失败时弹出错误气泡（`notify.ts` + `showPopup` 机制），不再静默吞异常。
- **TODO-6: 下游路径门禁强制执行** → `engine.py` / `backup_ops.py` 断言式门禁。在关键入口添加 `assert` 检查路径合法性（非空、非临时、格式合规），确保非法路径被提前阻断而非在深层报错。
- **TODO-11: aggregator 层路径 ~ 展开** → pipeline 路由 `resolve_file_path`。aggregated_rule_set 中 `user_config` / `rules` 路径在聚合层统一经过 `resolve_file_path` 展开 `~`，消除前端与后端之间 ~ 残留。
- **TODO-3: "仅分枝"空结果提示语修正** → 区分无树（"暂无森林图。请先点击'计算映射'。"）与有树无分支（"当前不存在待裁决的分支问题"），文案集中在 `locales/zh-CN.ts`。

### 阶段 B — UX 收尾（TODO-4/5/12/13）

- **TODO-4: BackupPage GUI 审查** → 补充 `created_at` 列显示、文件勾选恢复支持、错误提示气泡化。`backupPage` 的 STR 字符串补全（`createdAt`、`selectedFiles`、`noFilesSelected` 等）。
- **TODO-5: 全局路径显示规范** → `ensureTrailingSlash` 补漏。在所有目录路径展示位（DataSource、Forest、Settings）统一尾部斜杠，消除 `steamapps` vs `steamapps/` 不一致。
- **TODO-12: Rules paths 前端预检** → 标记完成。后端的路径校验已经就位（`resolve_file_path` 检查存在性），前端无需重复实现。
- **TODO-13: i18n 预备** → 130+ 字符串集中在 `frontend/src/locales/zh-CN.ts`。按页面/模块分组（ForestPage、ConflictsPage、RulesPage、BackupPage、DataSourcePage、SettingsPage、LayoutShell、ForestViewer、Stores、notify、errorCodes），语义化 key，组件中 `import { STR }` 引用。

### 阶段 C — 存储规范与 Schema（TODO-2/14）

- **TODO-2: /tmp/ 清理** → `DESIGN_STORAGE.md` 冻结全部存储分类与默认路径。区分持久数据（`~/.local/share/modmanager/`）与运行时数据（`/tmp/modmanager_*/`），定义 first-use 初始化流程与生命周期。
- **TODO-14: 3 份 JSON Schema 新建** → `user_config.schema.json`、`branch_decisions.schema.json`、`sse_event.schema.json`（2020-12 标准）。至此 repo_spec/ 共 8 份 Schema 全部就位。
- **新建 `DESIGN_COMM_PROTOCOL.md`** → REST + SSE 协议冻结。定义 API 端点风格、请求/响应格式、错误码规范、SSE 事件类型与 payload 结构。
- **新建 `DESIGN_STORAGE.md`** → 存储分类（持久/运行时/缓存）、first-use 初始化、持久化生命周期管理。
- **user_config 搜索策略改为单级 + 自动生成** → 不再递归搜索上层目录，改为在固定位置自动生成默认 `user_config.json` 并提供第一使用引导。

### 阶段 D — DataSource 去重（TODO-16/17/18）

- **TODO-16: managed + radio 去重** → `game_map` 改为 list-per-appid 结构，同一 appid 的多个 game 条目以列表形式共存；game 与 mod 各自独立 radio 组，互斥选择保留的库/路径。批量提交时根据 radio 选择决定保留项。
- **TODO-17: 表头 emoji → "可见性"** → DataSource 表头列名从眼睛 emoji 改为纯文本 `colVis: '可见性'`，保持专业风格。
- **TODO-18: W_DUPLICATE → `E_DUPLICATE_APPID` + 新增 `E_DUPLICATE_MIXED_ID`** → 区分 appid 级别重复与 mixed_id（`appid:modid`）级别重复，错误码集中定义于 `errorCodes.ts`，描述文案含排障指引。错误气泡点击展示详细排障指南。

### 阶段 E — mod 字段重命名与 GUI 重构

- **mod 字段全链重命名** → 涉及 21 个文件、~96 行变更。覆盖 Python 后端（model、schema、route）、前端（component、store、type、locale）、文档（DESIGN 文件）。
- **`database.schema.json` 新增 `Game.managed` + `Mod.managed`** → boolean 字段标记用户是否在 DataSource 页面选择了该条目，用于去重决策和批量提交。
- **GUI 7 Tab 重构** → 新建 `RulesOverview` / `Settings` / `Operations` / `RuleEditor` mock 页；`DataSource` / `Forest` / `Conflicts` / `Backup` / `Rules` 保留。路由表重构为 7 Tab 体系。
- **导航栏：设置移至末尾 + emoji 前缀** → 导航顺序调整为：数据源→规则概览→Forest→冲突裁决→文件操作→规则管理→备份恢复→设置。每项添加 emoji。

### 阶段 F — MOD 排序与 DataSource 完善

- **MOD 按 (appid, modid, path, lib_index) 排序** → 四键排序确保跨库同 appid+modid 的 MOD 相邻显示，便于人工比对去重。
- **MOD 库归属改用 modpath 前缀匹配** → 根据 mod 文件路径前缀匹配所属 library，而非此前不稳定的索引匹配。
- **E_DUPLICATE 错误气泡点击展示排障指南** → 气泡内容含具体重复数据与操作指引（"请在 MOD 表中勾选单选按钮"），可通过 `popup-link` 跳转到 DataSource 页面。
- **"保存当前选择"按钮** → DataSource 页面新增 `💾 保存当前选择` 按钮，仅保存 radio 选择不跳转版本，避免意外导航。
- **Database 副本生命周期** → localStorage 持久化 Database 副本，页面刷新保留；后端更新时同步。`persistence.ts` 统一管理 `modmanager:` 前缀 key。
- **数据库路径迁入设置页 + Forest 清理** → database path 从 Forest 页面移除，统一归入 Settings 页面管理；Forest 页面 database 行改为只读展示（从设置页继承）。
- **Settings 页从 mock 改造为功能页** → 实现保存按钮、database.json 高级编辑、路径配置、output_path 空值处理、`Path` 导入、`cache_path` 蛇形命名修复。
- **修复 Settings 保存 output_path 接受 null + Path 导入 + cache_path 蛇形命名** → 后端 schema 允许 `output_path: str | None`；Python 侧使用 `from pathlib import Path`；`cache_path` 统一蛇形命名风格。

## 测试

- **Python:** 368 passed（含 1 subtests）
- **前端 Vitest:** 71 passed（10 test files）
- **前端构建:** ✅ 成功（`vite build` 无报错，chunk 拆分：vue-vendor 108KB、index 136KB、DataSourcePage 45KB、ForestPage 16KB）

## 文档新建/更新

### 新建（7 份）
| 文档 | 说明 |
|------|------|
| `DESIGN_COMM_PROTOCOL.md` | REST+SSE 协议冻结 |
| `DESIGN_STORAGE.md` | 存储分类、first-use、生命周期 |
| `repo_spec/user_config.schema.json` | user_config JSON Schema |
| `repo_spec/branch_decisions.schema.json` | branch_decisions JSON Schema |
| `repo_spec/sse_event.schema.json` | sse_event JSON Schema |
| `frontend/src/locales/zh-CN.ts` | 130+ 中文 i18n 字符串集中定义 |
| `DESIGN_SESSION_2026-05-09.md` | 当日设计方案记录 |

### 更新（8 份）
| 文档 | 变更 |
|------|------|
| `DESIGN_GUI.md` | 7 Tab 路由、导航顺序、emoji 规范 |
| `DESIGN_GUI_DATASOURCE_TAB.md` | managed/radio 去重机制、MOD 排序规则、库归属前缀匹配 |
| `DESIGN_STORAGE.md`（更新） | 冻结论述补全 |
| `DESIGN_REST_API.md` | database 类型 `Any` → `dict \| str` |
| `DESIGN_ENGINE_INVARIANTS.md` | 门禁断言条目补充 |
| `DESIGN_PATH_RESOLVER.md` | ~ 展开在 aggregator 层的位置说明 |
| `DOCUMENT_GOVERNANCE.md` | 文档状态跟踪 |
| `database.schema.json` | 新增 `Game.managed` + `Mod.managed` 字段 |

## TODO 消化

| TODO | 状态 | 说明 |
|------|------|------|
| TODO-2 | ✅ | /tmp/ 清理 + DESIGN_STORAGE.md 冻结 |
| TODO-3 | ✅ | "仅分枝"空结果提示语修正 |
| TODO-4 | ✅ | BackupPage GUI 审查补全 |
| TODO-5 | ✅ | 全局路径显示规范 + ensureTrailingSlash |
| TODO-6 | ✅ | 下游路径门禁强制执行 |
| TODO-8 | ✅ | ConflictsPage branch decisions 持久化 |
| TODO-9 | ✅ | onDbPathBlur 静默失败修复 |
| TODO-11 | ✅ | aggregator 层 ~ 展开 |
| TODO-12 | ✅ | Rules paths 前端预检完成（后端已有） |
| TODO-13 | ✅ | i18n 预备 130+ 字符串集中 |
| TODO-14 | ✅ | 8 份 JSON Schema 全部就位 |
| TODO-16 | ✅ | DataSource managed+radio 去重 |
| TODO-17 | ✅ | 表头 "可见性" 替换 emoji |
| TODO-18 | ✅ | W_DUPLICATE → E_DUPLICATE_APPID + E_DUPLICATE_MIXED_ID |

## 后续待办（states.md 保留）

- TODO-10: （挂起）空输入校验
- TODO-15: Forest SVG 自适应视窗缩放 + 视窗功能增强
- TODO-19~26: 7 项挂起项（UI 提示、选项卡解耦、偏好保存、路径迁入、backup dir 语义、dry run 迁移、手动路径列表）

## 完成清单（from work_memo/states.md）

- [x] 阶段 A：Bug 修复（TODO-8/9/6/11/3）
- [x] 阶段 B：UX 收尾（TODO-4/5/12/13）
- [x] 阶段 C：存储规范与 Schema（TODO-2/14）
- [x] 阶段 D：DataSource 去重（TODO-16/17/18）
- [x] 阶段 E：mod 字段全链重命名 + GUI 7 Tab 重构
- [x] 阶段 F：MOD 排序 + DataSource 完善 + Settings 功能化
- [x] Python 368 tests
- [x] 前端 71 tests / 10 files / build 成功
- [x] 新建 7 份 + 更新 8 份设计文档
- [x] 新建 3 份 JSON Schema
- [x] i18n 预备（130+ 字符串）
