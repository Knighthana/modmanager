# 2026-05-10 工作日志

## 完成事项

### 热修复：managed 属性全链路修复（4 轮）

- **E_PATH_GATE_DIR**：`database_ops.py` 中 8 处目录路径追加尾部 `/`（basepath、modpath、mod.path）
- **managed 全部为 false**：`_build_mod_from_games` 从 `old_mod` 恢复 managed；`_scan_from_libraries` 接受 `old_games` 参数传递旧值；`liveupdate_database` / `regen_database` 传入旧数据
- **managed 保存后前端不同步**：`doSave()` 成功后调用 `store.updateDatabase(db)` 更新 datasource store 本地状态
- **managed 语义闭环**：`validate_database` 仅对 managed=true 的条目检查唯一性；`_scan_from_libraries` 无重复 appid 自动 managed=true；`_populateFromDatabase` 错误替换而非追加
- **E_DUPLICATE 残留清除**：`/database/save` 条件清除（仅当该类型全部重复组都被 managed 解决）；响应返回 `data.database` 供前端同步
- **Settings 页 save 按钮修复**：前端使用 `resp.data.database` 覆盖本地副本

### 方案迁移：CSS transform → svg-pan-zoom

- `ForestViewer.vue` 重写（260→222 行），删除手写 ~100 行 zoom/pan/drag/fit 逻辑
- 初始化时序修复（`nextTick` + `requestAnimationFrame` 确保 `clientWidth` 可用）
- SVG 塌陷 CSS 修复（`.forest-container :deep(svg) { width:100%; height:100% }`）
- 容器高度匹配缩放后 SVG（`height` 替代 `min-height`，居中顺序修正）
- ResizeObserver 延迟创建修复

### 新增功能：重置按钮 + 小地图

- 工具栏："重置视图"按钮 → `fit()` + `center()`
- 左上角 180×120 半透明小地图，矩形表示法（全图区域 + 蓝色视口框）
- 点击小地图 → 主视图 pan 跳转
- 小地图显示修复（`v-html` 不吞子元素 + `panZoomReady` ref 响应式）
- 小地图 SVG 克隆改为矩形表示（性能优化）

### TODO 完成

- **TODO-15**：Forest SVG 自适应视窗宽度缩放 + svg-pan-zoom 迁移 + 重置按钮 + 小地图
- **TODO-21**："发现模式"偏好保存——扫描按钮按下后调用 `saveToCache()` 持久化

### 文档更新

- `DESIGN_STORAGE.md`：managed 初始值修正（无重复自动 true）、管道消费过滤说明、废弃决策 D4 标记、单副本原则新增、§8.4 补 `datasource-db` key + 权威性声明
- `DESIGN_GUI.md`：svg-pan-zoom 方案对比表 + 小地图/重置按钮 + 自适应宽度高度
- `DESIGN_GUI_DATASOURCE_TAB.md`：自动 managed=true + 管道消费过滤段落
- `DESIGN_ENGINE_INVARIANTS.md`：新增 managed 过滤行为章节
- `DESIGN_COMM_PROTOCOL.md`：补 `/api/database/save` 端点
- `DESIGN_REST_API.md`：补 `/api/database/save` 请求/响应 schema + 同步语义
- `direct/` 下 HOTFIX-1/2/3/4 状态更新为"已实现"
- `direct/` 下 MIGRATE-svg-pan-zoom.md + ENHANCE-minimap-and-reset.md 状态更新

## 新增 TODO

- **TODO-27**：pipeline compute 数据流修复——后端自行从文件读 database
- **TODO-28**：前端 database 副本合并——3 个 localStorage key 合并为 1 个
- **TODO-29**：Settings 页 Database JSON 保存按钮修复（缺 output_path + 未同步 response）
- **TODO-30**：GUI 页面实现策略——讨论 mock-first 是否可行

## 设计文档产出

- `direct/TODO-15-step1-svg-fit-viewport.md`
- `direct/HOTFIX-path-trailing-slash-and-managed.md`
- `direct/HOTFIX2-managed-frontend-sync.md`
- `direct/HOTFIX3-managed-semantics-closure.md`
- `direct/HOTFIX4-v2-duplicate-cleanup-and-svg-height.md`
- `direct/MIGRATE-svg-pan-zoom.md`
- `direct/ENHANCE-minimap-and-reset.md`
- `direct/TODO-22-migrate-fields.md`

## 修改文件统计

| 类型 | 文件数 |
|------|--------|
| Python 后端 | 4（database_ops.py, validation.py, routes/database.py, test_database_ops.py） |
| Vue 前端 | 4（ForestViewer.vue, DataSourcePage.vue, datasource.ts, zh-CN.ts） |
| 设计文档 | 8（DESIGN_STORAGE/GUI/GUI_DATASOURCE_TAB/ENGINE_INVARIANTS/COMM_PROTOCOL/REST_API + 6 个 direct/） |
| 工作状态 | 2（work_memo/states.md, repo_logs/2026-05-10.md） |
